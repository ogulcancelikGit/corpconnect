# Frontend ortam değişkenleri — bu dosyayı .env olarak kopyalayın
# API taban adresi. Tanımlanmazsa kod http://localhost:5000/api değerine düşer.
# Geliştirmede Vite, /api ve /uploads yollarını backend'e (5000) proxy'ler.
VITE_API_URL="http://localhost:5000/api"
